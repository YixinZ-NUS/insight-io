{
  "version": 1,
  "features": [
    {
      "id": "runtime-build-and-test",
      "category": "operational",
      "description": "A developer can configure, build, and run the standalone insight-io backend targets",
      "steps": [
        "Run CMake configure from the repo root",
        "Build the backend binaries and focused tests",
        "Verify the expected binaries are produced",
        "Run the focused test targets successfully"
      ],
      "passes": false
    },
    {
      "id": "backend-health-check",
      "category": "operational",
      "description": "An operator can start the backend and verify the health endpoint",
      "steps": [
        "Start the backend with a db path and frontend path",
        "Call GET /api/health",
        "Verify status is ok",
        "Verify the backend version is returned"
      ],
      "passes": false
    },
    {
      "id": "catalog-lists-single-stream-uris",
      "category": "functional",
      "description": "The device catalog lists canonical URIs where each URI represents exactly one stream",
      "steps": [
        "Call GET /api/devices",
        "Inspect listed URI entries",
        "Verify each URI corresponds to one stream",
        "Verify separate color and depth streams are listed as separate URIs when both exist"
      ],
      "passes": false
    },
    {
      "id": "device-alias-produces-human-usable-uri",
      "category": "functional",
      "description": "An operator can alias a discovered device and then use the aliased name in canonical URIs for direct sessions or app routes",
      "steps": [
        "Inspect the catalog for a discovered device default name",
        "Call the device alias endpoint",
        "Verify GET /api/devices shows the aliased public name",
        "Verify later session or app-source requests can use the aliased name in the URI"
      ],
      "passes": false
    },
    {
      "id": "direct-session-create-via-rest",
      "category": "functional",
      "description": "A user can create a direct logical session through POST /api/sessions without going through the app layer",
      "steps": [
        "Call POST /api/sessions with a listed device name, preset, and delivery",
        "Verify the session response is active",
        "Verify the returned payload includes session and stream metadata",
        "Verify the session can be inspected through GET /api/sessions/{id}"
      ],
      "passes": false
    },
    {
      "id": "shared-capture-rtsp-and-ipc-reuse",
      "category": "functional",
      "description": "Two same-preset sessions can reuse shared capture while exposing separate delivery intents",
      "steps": [
        "Create an RTSP session for a listed camera preset",
        "Create an IPC-oriented session for the same camera preset",
        "Inspect runtime status",
        "Verify shared capture reuse is visible while each logical session remains distinct"
      ],
      "passes": false
    },
    {
      "id": "insightos-open-equivalent-session-flow",
      "category": "functional",
      "description": "A thin opener command can still create a direct session from one canonical URI",
      "steps": [
        "Run the opener binary with one canonical URI",
        "Observe the created session id",
        "Inspect the created session through REST",
        "Verify the same flow is representable through POST /api/sessions"
      ],
      "passes": false
    },
    {
      "id": "persisted-session-restart-after-backend-exit",
      "category": "functional",
      "description": "A stopped direct session can survive backend exit and be rehydrated explicitly after restart",
      "steps": [
        "Create and stop a direct session",
        "Stop the backend",
        "Restart the backend with the same database",
        "Inspect the persisted session record",
        "Call POST /api/sessions/{id}/start",
        "Verify a fresh runtime session is active"
      ],
      "passes": false
    },
    {
      "id": "idle-app-registers-and-exposes-app-id",
      "category": "functional",
      "description": "An idle app can connect without initial sources and expose an app id for later route connection",
      "steps": [
        "Start an app without any initial source URI",
        "Call GET /api/apps",
        "Find the running app record",
        "Verify the app id can be used for later route and source operations"
      ],
      "passes": false
    },
    {
      "id": "idle-app-adds-frame-routes-later",
      "category": "functional",
      "description": "A running idle app can declare multiple independent video-purpose routes after startup",
      "steps": [
        "Create or locate an idle app",
        "Create route yolov5 with media expectation video",
        "Create route yolov8 with media expectation video",
        "List app routes",
        "Verify both routes are present and distinct"
      ],
      "passes": false
    },
    {
      "id": "idle-app-adds-related-routes-later",
      "category": "functional",
      "description": "A running idle app can declare related routes with grouped-source expectations after startup",
      "steps": [
        "Create or locate an idle app",
        "Create route scene-color with media expectation video",
        "Create route scene-depth with media expectation depth and same_group_as scene-color",
        "List app routes",
        "Verify both routes are present and distinct"
      ],
      "passes": false
    },
    {
      "id": "late-connect-frame-source-to-declared-route",
      "category": "functional",
      "description": "A running idle app can receive a later video URI connection bound to a declared route",
      "steps": [
        "Start an idle app and declare route yolov5",
        "Connect a listed compatible video URI with route yolov5",
        "Verify the source record is created",
        "Verify a logical session is linked",
        "Verify the app receives route-scoped callbacks"
      ],
      "passes": false
    },
    {
      "id": "late-connect-color-source-to-declared-route",
      "category": "functional",
      "description": "A running idle app can receive a later grouped-source URI connection bound to a declared route",
      "steps": [
        "Start an idle app and declare route scene-color",
        "Connect a listed URI such as insightos://host/desk-rgbd/480p_30?source=color with route scene-color",
        "Verify the source record stores the resolved source stream identity",
        "Verify the app receives route-scoped callbacks"
      ],
      "passes": false
    },
    {
      "id": "late-connect-depth-source-to-declared-route",
      "category": "functional",
      "description": "A running idle app can receive a later grouped-source URI connection bound to a declared route",
      "steps": [
        "Start an idle app and declare route scene-depth",
        "Connect a listed URI such as insightos://host/desk-rgbd/480p_30?source=depth with route scene-depth",
        "Verify the source record stores the resolved source stream identity",
        "Verify the app receives route-scoped callbacks"
      ],
      "passes": false
    },
    {
      "id": "mixed-app-video-plus-related-group-routing",
      "category": "functional",
      "description": "One app can run a video route plus related grouped-source routes at the same time with independent routing",
      "steps": [
        "Create one app",
        "Declare one video route, one scene-color route, and one scene-depth route",
        "Connect one video URI plus grouped-source URIs for color and depth",
        "Verify all routed sources remain active independently"
      ],
      "passes": false
    },
    {
      "id": "single-uri-launch-still-works-for-single-route-apps",
      "category": "functional",
      "description": "A single-route app can launch with one bare URI argument and immediately consume from it",
      "steps": [
        "Build or run an app binary that declares exactly one route",
        "Pass one canonical URI as a CLI argument",
        "Verify the CLI connects that URI to the declared route",
        "Verify the app receives the expected route callbacks"
      ],
      "passes": false
    },
    {
      "id": "multi-uri-launch-supports-explicit-route-connections",
      "category": "functional",
      "description": "A multi-route app can launch from CLI by connecting URIs to route names explicitly",
      "steps": [
        "Build or run an app that declares multiple routes",
        "Launch it with explicit route-to-URI CLI connections",
        "Verify each route receives the correct source",
        "Verify no post-launch REST injection is required for that startup path"
      ],
      "passes": false
    },
    {
      "id": "source-response-exposes-route-and-source-stream",
      "category": "functional",
      "description": "App-source responses expose route metadata and resolved source identity clearly enough for the SDK to attach",
      "steps": [
        "Create an app and declare a route",
        "Connect a compatible source",
        "Inspect the source response",
        "Verify route, resolved_source_id, resolved_source_member, resolved_source_group_id when present, and session metadata are present"
      ],
      "passes": false
    },
    {
      "id": "app-source-stop-and-start-preserve-declaration",
      "category": "functional",
      "description": "Stopping and restarting an app source preserves the durable app-source declaration while recreating runtime state",
      "steps": [
        "Create an app, route, and source",
        "Call the app-source stop endpoint",
        "Verify the app-source row remains present and moves to stopped",
        "Call the app-source start endpoint",
        "Verify a fresh runtime session is linked to the same app-source record"
      ],
      "passes": false
    },
    {
      "id": "browser-catalog-to-route-builder-flow",
      "category": "functional",
      "description": "The browser UI lets a user move from catalog inspection to app creation, route declaration, and source connection",
      "steps": [
        "Open the browser UI",
        "Inspect the listed canonical URIs",
        "Create an app",
        "Create one or more routes",
        "Connect a listed URI to a selected route",
        "Verify the routed source appears in the app detail view"
      ],
      "passes": false
    },
    {
      "id": "browser-restart-recovery-flow",
      "category": "functional",
      "description": "The browser UI can show persisted app and source declarations after backend restart and let the user restart them explicitly",
      "steps": [
        "Create an app, route, and source from the UI",
        "Restart the backend with the same database",
        "Reload the UI",
        "Verify the persisted app and source declarations are still listed",
        "Restart the source from the UI",
        "Verify status updates reflect the fresh runtime session"
      ],
      "passes": false
    }
  ]
}
